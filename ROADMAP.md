# Franchise Football — Roadmap & Execution Playbook

**Read this after HANDOFF.md, before starting any new chunk of work.** HANDOFF.md is the detailed "what happened and why" log. This file is the forward-looking "what's left and how to spend tokens efficiently doing it" plan. Update the checkboxes here as chunks land; leave HANDOFF.md's own numbered-item narrative style for the detailed record of *how* each chunk was actually built.

Written 2026-09-09, after item 37 (roster-depth rotation); updated repeatedly the same day through M1-M9 (original MVP chunks) and M10-M15 (the Figma re-audit's correction chunks -- see §2b for the full account, including two real data-persistence incidents found and fixed along the way). Current state: 248 tests passing (1 skipped) as of the last full run — **M1-M15 are all done.** Most recently updated after adding GDD §12 (Headlines Feature) and its own new R-series row below (§4).

---

## 1. Where things stand

**Solid and real, not stubs:** full sim engine (drive/game sim, play-calling AI, Score Fidelity System), season/schedule/standings/Power Ratings, real playoffs with the full tiebreaker chain, Awards (MVP/OPOY/DPOY/ROY), Progression/Regression, save/load, League History, real Hall of Fame, Player Cards (Overview/Ratings), real NFL data seeding (2002-2025), roster-depth rotation (item 37), and a UI that matches the Figma design tokens with a persistent header on every page.

**What's actually left for MVP** (Part 1 per the GDD) is a short, specific list — not a vague "polish everything":

1. ~~Defensive TD~~ — **done (M1, 2026-09-09)**. See §2's M1 row and HANDOFF.md item 38.
2. ~~Kicking/Punting box-score stats~~ — **done (M2, 2026-09-09)**. See §2's M2 row and HANDOFF.md's M2 session entry.
3. ~~Two UI pages that still don't match the Figma redesign: Roster, HOF~~ — **done (M4 + M5, 2026-09-09)**. ~~Stats (customizable columns)~~ — **done (M3, 2026-09-09)**, see §2's M3/M4/M5 rows and HANDOFF.md items 39/M4/M5.
4. ~~Player Card's Stats tab~~ — **done (M6, 2026-09-09)**. ~~Contract tab~~ — **done (M8, 2026-09-09)**, see §2's M6/M8 rows.
5. ~~Dashboard page still doesn't match the Figma 3-column grid layout~~ — **done (M9, 2026-09-09)**. See §2's M9 row.
6. A handful of disclosed, smaller stat-realism gaps (DEF tackles still ~2-3x real, TFL still elevated) that `tests/test_stat_realism.py` now tracks with deliberate headroom rather than hides.

Everything else — Contracts, Free Agency, Trades, Coaching Staff, Draft, weather, the full penalty catalog, a real injury system, return-game simulation — is Part 2 (R2) per the GDD's own scope line, confirmed multiple times this project: *"Everything in this Part is in scope for the first playable release. Nothing here should expand without an explicit decision to promote content from Part 2."*

---

## 2. MVP chunks (finish Part 1)

Each row is sized to run as its own fresh Claude Code session (or bundled per the Bundling column). "Model" is a recommendation, not a requirement — see §4 for the reasoning.

| # | Chunk | What it actually is | Key files | Size | Model | Bundle with |
|---|---|---|---|---|---|---|
| **M1** | ~~Defensive TD~~ **DONE (2026-09-09)** | New `PlayEvent` outcome (`"defensive_touchdown"`). Scope stayed narrow as written: no open-field return simulation — `drive_sim.py`'s `_defensive_td_probability()` rolls a real, small, distance-based chance at every INT/fumble. Wired into `defensive_box_score.py` (new `defensive_touchdowns` field), `awards.py`'s DPOY weights (rebalanced, +0.10 slice), and season/career stats (`season_stats.py`, `history_store.py`, including its own HOF defensive scoring). Also fixed a real box_score.py stat-corruption bug the new outcome exposed (would have double-counted a pick-six as a real reception) and a `test_stat_realism.py` headroom fragility the new RNG draw exposed at the suite's default seed — see HANDOFF.md item 38 for full detail. | `drive_sim.py`, `game_state.py`, `defensive_box_score.py`, `awards.py`, `game_sim.py`, `box_score.py`, `season_stats.py`, `history_store.py` | M | Sonnet (no Opus session needed — mechanic stayed within the narrow scope as written) | — |
| **M2** | ~~Kicking/Punting box score~~ **DONE (2026-09-09)** | New `KickingLine`/`PuntingLine` in `box_score.py`, keyed by real kicker/punter like `RushingLine` (FG made/attempted by distance bucket via the same desc-text parsing convention `scouting.py`'s `field_goal_accuracy()` already used, since attempt yardage isn't in a structured field; XP made/attempted via a new `extra_point` `PlayEvent` play_type, since PATs used to be folded silently into the touchdown event with no independent per-kicker attribution; punt count + **net** avg (not gross — no return-game sim exists, same disclosed gap as `scouting.py`'s own return-average note) + inside-20). `depth_chart.py`'s `OffensiveStarters` gained a real `p` (punter) field alongside the existing `k`. Wired into `result.html`'s box score tables. One disclosed gap: a Defensive TD's own PAT has no real kicker object available in that code path (it belongs to the defense's team, not the drive's offense), so it's excluded from the Kicking line rather than guessed. Full suite: 239 passed, 1 skipped, run twice. See HANDOFF.md for full detail. | `box_score.py`, `drive_sim.py`, `depth_chart.py`, `game_state.py`, `result.html` | M | Sonnet | — |
| **M3** | ~~Stats page redesign~~ **DONE (2026-09-09)** | Team/Player/Coach tabs, sortable columns, a customizable-column chooser (real Figma source: `docs/figma-export/src/app/components/StatsPage.tsx` + `stats/StatColumnChooser.tsx`), translated into this project's existing GET-query-param + full-page-reload pattern (no htmx/framework) rather than the source's React client state. The stat catalog is deliberately trimmed to only fields this engine actually attributes (`season_stats.py`/`defensive_box_score.py`/`TeamRecord`) — no fabricated air-yards/pressure/snap-count columns. Coach tab renders "Coming Soon" (no Coach entity). Export button is a stub alert, per scope. Old fixed Passing/Rushing/Receiving/Defensive leaderboard tables are gone, replaced by the Player tab; Awards Race (a GDD-original addition, not from the Figma export) is unchanged. See HANDOFF.md item 39. | `app/templates/stats.html`, `app/templates/base.html` (new CSS only), `app/main.py`'s stats route | M | Sonnet (session ran at Sonnet, not Haiku — still mechanical, no redesign beyond scope) | — |
| **M4** | ~~Roster page redesign~~ **DONE (2026-09-09)** | Attributes/Stats view toggle, Team Quota badges, Filter/Export controls, embedded depth-chart widget (§10.4.2). Real source: `docs/figma-export/src/app/components/RosterPage.tsx`/`RosterTable.tsx`. | `app/templates/roster.html`, `depth_chart.html` (for the embed) | M | Sonnet | own session |
| **M5** | ~~HOF page redesign + League Record Book/Super Bowl History~~ **DONE (2026-09-09)** | Real `/hof` redesign per GDD Sec 10.4.8: a "Class of Season N Inductees" highlight (diffed against the prior archived season), an "Eligible Candidates" list (past `MIN_HOF_SEASONS`, below the induction threshold, ranked by a disclosed simplified "how close" proxy), filterable/searchable Hall of Fame Members, a League Record Book (top-10 all-time leaders per category from `career_stats()`; Kicking omitted, not archived at the career level), and a Super Bowl History table (champion + top seeds only — runner-up/score/SB MVP aren't archived per-season, disclosed rather than fabricated or built out, since that would mean extending `archive_season()`'s schema). Deliberately made zero changes to `history_store.py` (a concurrent M6 session was actively editing `career_stats()` there) — built entirely by composing its existing public functions. Found and fixed a real bug via its own new tests: the "new this season" diff was returning the PRIOR season's inductee keys unchanged instead of actually diffing them. See HANDOFF.md's M5 session entry, including its own three-way concurrent-session commit-attribution note (mirroring M6's — most of this chunk's code landed via M4's commit `544cf95b`, not its own). | `app/templates/hof.html`, `app/templates/base.html` (new CSS only), `app/main.py`'s hof route | S–M | Sonnet | — |
| **M6** | ~~Player Card Stats tab~~ **DONE (2026-09-09)** | Wired `history_store.career_stats()` (already real, already tested) into the Player Card's Stats panel via a new `_career_stats_for()` helper, keyed the same `(team_abbr, name)` way `career_stats()` itself is. Also added real caching (`career_stats()` was ~230ms uncached against the real 24-season history file, and the card now calls it once per player render) via the same lru_cache-plus-explicit-clear pattern `depth_chart.py` already uses, invalidated by `season_state.start_new_season()`. See HANDOFF.md's M6 session entry, including a real three-way concurrent-session commit-attribution note (the code landed correctly, but bundled into M4's commit `544cf95b` rather than its own). | `app/main.py`'s `_player_card_json`, `base.html`'s modal JS, `history_store.py`, `season_state.py` | S | Sonnet | — |
| **M7** | *(not an MVP chunk — see note)* | Originally drafted as "further DEF-tackle stat-realism calibration" (the disclosed ~2-3x-real gap from item 37) while writing this roadmap, then cut before the table was finalized because closing it for real needs special-teams tackle simulation, which is R2 scope (§4's R2 row) — not something an MVP-scoped session should attempt. The row got deleted but M8 was never renumbered down, leaving this gap. Left as a placeholder rather than renumbering M8, since HANDOFF.md and earlier commits already cite "M8" by that name. | — | — | — | — |
| **M8** | ~~Player Card Contract tab~~ **DONE (2026-09-09)** | §3's premise turned out to be stale: `Player.salary`/`Player.signing_bonus` (`app/models/player.py`) already store real, non-fabricated Madden-derived dollar figures imported from the roster CSV — just never wired to any UI. Brian picked the newly-surfaced third option over §3's original (a)/(b): show the real salary/signing bonus, with an honest disclosure that contract length/years-remaining/cap space still aren't modeled (no fabrication either way). `_player_card_json()` now includes both fields; `base.html`'s modal JS renders them with `Intl`-less `toLocaleString` currency formatting. Found and fixed a real, previously-invisible bug while building this: `scripts/import_players.py` looked up the CSV's salary columns by the wrong key (missing a leading space the real headers carry — `" Total Salary "`/`" Signing Bonus "`, not `"Total Salary "`/`"Signing Bonus "`), so `dict.get()` silently missed on every row and every imported player's salary/signing_bonus had been sitting at the default of 0 in every existing save, including Brian's live 8-2 KC franchise. Fixed the import script for future/fresh imports, then repaired the live save in place with a one-off script that recomputed the exact same deterministic `player_id` (`build_player()`'s own `_slug()`) from the CSV and UPDATEd only the salary/signing_bonus columns on the matching existing row — verified all 2365 players matched with zero identity mismatches before committing, so no progression/rating/team data was touched. New regression test `test_imported_players_have_real_salaries` in `test_player_model.py` (confirmed it fails against the pre-fix DB, passes after). Full suite: 248 passed, 1 skipped, run twice (touched a real data-correctness bug, not just UI, so didn't use the pure-UI single-run tier). Verified live in-browser (KC franchise, week 11): Mahomes' Contract tab shows real $190,400,000 salary / $140,770,000 signing bonus. | `app/main.py`'s `_player_card_json`, `base.html`'s modal JS + contract panel, `scripts/import_players.py`, `tests/test_player_model.py` | S | Sonnet | — |
| **M9** | ~~Dashboard page redesign~~ **DONE (2026-09-09)** | Real spec: `docs/figma-export/src/app/DASHBOARD_DOCUMENTATION.md` (lines ~43-59), a 3-column CSS Grid — Row 1: Division Standings \| Team Schedule \| Scouting Panel (3 equal columns); Rows 2-3: left column has League Power Rankings/Top Performers stacked, right two columns hold Box Score & Play-by-Play. Most of the 8 named widgets needed a real (if small) new query, not just a moved `<div>` — `app/main.py` gained `_team_schedule_for()` (per-team schedule, replacing the old league-wide "last week's results" card) and `_last_played_game_for()` (the user's own most recent completed game, reusing `result.html`'s existing `build_box_score`/`build_defensive_box_score` calls for its Box Score/Play-by-Play widgets), plus a division-filtered standings list and a power-rating-sorted list (both re-sorts/filters of already-real `season.records` data, no new fields). Weekly Gameplan stayed outside the grid (GDD-original addition, same precedent as Stats' Awards Race/HOF's Record Book). CSS-only grid/span/scroll classes added to `base.html`. See HANDOFF.md's M9 session entry. | `app/templates/dashboard.html`, `app/templates/base.html` (new grid CSS), `app/main.py`'s dashboard route | M | Sonnet | — |

**MVP complete: M1-M9 all done.** No remaining *net-new* MVP chunks — see §4 for the R-series (Post-MVP) work. **§2b's Figma re-audit correction chunks are now ALL done: M10 (Dashboard), M11 (Roster), M12 (Playoffs), M13 (Stats), M14 (HOF), M15 (Depth Chart).** See §2b's own notes for two real data-persistence incidents (and their fixes) discovered along the way -- read those before writing any future standalone verification script that touches `season_state`/`app.core.db`.

**IMPORTANT for any session running the test suite:** `tests/conftest.py` now isolates the real Season save file (`data/saves/current_season.json`) for the whole pytest run -- read that file's own docstring before touching `season_state.py`'s save/load path or `save_service.DEFAULT_SAVE_PATH`. Before this fix (discovered during M12), running `pytest tests/` with a real franchise save on disk would silently overwrite it, with no error and no warning -- the live app server never noticed because it only reads the save once per process.

**IMPORTANT, second incident (discovered during M14, real damage occurred): there is a THIRD persistence surface, `app.core.db.DB_PATH` (`data/franchise_football.db`, the real Player roster/ratings database), separate from both `save_service.DEFAULT_SAVE_PATH` and `history_store.DEFAULT_PATH`.** `season_state.start_new_season()` calls `apply_progression_to_roster()`, which ages/re-rates every rostered player and commits it straight to whatever `app.core.db.DB_PATH` currently points to. A standalone verification script (run OUTSIDE pytest, to produce real HOF inductees for M14's testing) redirected the season-save and history paths correctly but never knew this third path existed -- it ran `start_new_season()` several times against the REAL, live `data/franchise_football.db`, with no in-memory cache anywhere to fall back on (unlike the M12 incident, where another process's cached season state made recovery possible). **Actual damage: 93% of all 2,365 players ended up at Overall Rating 0, 97% at age 45+.** No git history existed for this file (gitignored) and no backup was found anywhere. Recovered the ONLY way available -- re-ran `scripts/import_players.py` to rebuild the roster from the real CSV baseline (`data/raw/rosters/`) -- which restores a fully sane, playable database but **permanently loses every bit of in-game progression/regression/aging the franchise had accumulated across its real simulated seasons** (a previous session, M8, had explicitly avoided this exact tradeoff for this exact reason; this time it was already unavoidable, since the corruption had already destroyed that history before the tradeoff was even a choice). The corrupted DB was backed up before the fix in case anything is salvageable from it, but nothing in this repo currently reads it.

**Good news confirmed while investigating:** the pytest suite itself was NEVER at risk from this -- `test_season_rollover.py` and `test_history_store.py` (the only 2 files calling `start_new_season()`/`apply_progression_to_roster()`) both already correctly copy the real DB to a throwaway file and redirect `db_module.DB_PATH` per-test, restoring it afterward regardless of outcome. This was purely a standalone-script hygiene gap, not a test-infrastructure one -- **any future one-off verification script that calls `season_state.start_new_season()` (directly or via simulating a full season through playoffs+archival) MUST manually redirect all THREE of `save_service.DEFAULT_SAVE_PATH`, `history_store.DEFAULT_PATH`, AND `app.core.db.DB_PATH` (via `db_module.DB_PATH = <throwaway>; db_module._engine = None` to force the engine to rebuild against it) BEFORE importing/using `season_state` at all** -- see `test_season_rollover.py`'s own `_isolated_db_and_saves` fixture for the exact, correct pattern to copy.

**Note on concurrent sessions:** while M3 was being built, a separate concurrent session was independently working on M2 (Kicking/Punting box score) in the same local working directory, per this very playbook's own "one chunk = one fresh session" guidance. M3's session found M2's in-progress, uncommitted changes to `drive_sim.py`/`box_score.py`/`depth_chart.py`/`result.html` sitting in the working tree, confirmed they were unrelated to M3 (git-stash-verified the one test failure they caused was pre-existing on clean HEAD), and committed only its own three files rather than sweeping up someone else's in-flight work. If you're running multiple sessions in parallel like this, expect the same — check `git status` for files outside your chunk's Key Files column before committing.

**Update (M4/M5/M6, same day):** a rougher three-way version of the same situation — M4, M5, and M6 all ran concurrently and all three touched shared files (`app/main.py`, `app/templates/base.html`). File-level separation (M2/M3's fix above) doesn't help when the SAME file is shared; M6's session had to split individual diff hunks by hand (`git apply --cached` on a hand-built partial patch) to stage only its own changes. Even that wasn't enough at commit time: M4's session ran its own `git add`/`git commit` in the few-second gap between M6's staging and its own `git commit` call, and M4's commit (`544cf95b`) ended up including M6's already-staged hunks too (code landed correctly either way — nothing lost or corrupted — but M6's changes are attributed to M4's commit message instead of their own; see HANDOFF.md's M6 entry for the full account). M5's own hof_view/helper code got swept into that same `544cf95b` commit for the identical reason (see HANDOFF.md's M5 entry) — M5's own follow-up commit (`f09a73b6`) is mostly a real bug fix plus the template/test work, not the original feature landing. **If you're about to run several genuinely-concurrent sessions against overlapping Key Files** (not just the same repo), know that there is no way to fully guard against this from a single session's side — the best available mitigation is checking `git log`/`git show --stat` immediately after your own commit to confirm what actually landed, not just trusting that your `git add`/`git commit` sequence was atomic against the rest of the working directory. **Also watch for a stash appearing in `git stash list` that you didn't create** — M5's session hit exactly that mid-session (another concurrent session stashed the whole working tree, then unstashed/committed before M5 could act); it's a coordination move by another session, not a local accident, and `git stash apply` (not `pop`, to keep the entry as a backup) is the safe way to recover your own in-flight changes from it.

---

## 2b. MVP correction chunks (found via Figma re-audit, 2026-09-09)

**Root cause:** M9 (Dashboard) was built by reading `docs/figma-export/src/app/DASHBOARD_DOCUMENTATION.md` — a hand-written markdown summary — instead of the actual, current Figma component source tree (`docs/figma-export/src/app/App.tsx` + `components/*.tsx`), which is richer and in places structurally different (e.g. the doc omits `WeeklyGameplanBox` from the dashboard grid entirely; the real `App.tsx` puts it in column 1). Brian caught this by comparing the live app against a real Figma prototype screenshot. Since the same class of mistake could affect any chunk that had a `*_DOCUMENTATION.md` doc (or was built without one at all), every other page got the same audit treatment (8 parallel subagents, one per page, each told to treat the `.tsx` files as authoritative over any `.md`). Results:

- **Real deviations found, need correction:** Dashboard (M9), Roster (M4), Playoffs, Stats (M3), HOF (M5), Depth Chart. See M10-M15 below.
- **Correctly NOT built yet, no action needed:** Staff, GM Desk, Draft are intentional `coming_soon.html` stubs behind Part 2 systems (Coach entity, salary cap/contracts, trades) that genuinely don't exist in the engine — this is documented, disclosed scope, not a doc-mismatch bug. Draft's placeholder blurb does lean on a stale doc description of the superseded `DraftPage.tsx` (V1) rather than the actually-wired `DraftPageV2.tsx`, but since it's inert placeholder copy behind a stub, that's a footnote for whoever eventually builds R5, not its own chunk.

**For future sessions: when a `*_DOCUMENTATION.md` file exists under `docs/figma-export/src/app/`, read it for orientation only — verify every structural claim against the actual `.tsx` files in `components/` before building from it.** Several of these docs (Dashboard's confirmed, Draft's confirmed) are stale summaries of an earlier design iteration, not the live-wired one.

| # | Chunk | What it actually is | Key files | Size | Status |
|---|---|---|---|---|---|
| **M10** | ~~Dashboard correction~~ **DONE (2026-09-09)** | Real source: `App.tsx` (not the stale `DASHBOARD_DOCUMENTATION.md` M9 used). Moved Weekly Gameplan into column 1 (`.dashboard-col1-stack` now holds Division Standings/League Power Rankings/Top Performers below it, matching `App.tsx:117-122`'s `row-start-2 row-span-2` column instead of M9's one-per-grid-row scatter). Rebuilt Scouting Panel as a real tabbed view (Overview/Offense/Defense/Special/Discipline/Stats — new `.dash-tab`/`.dash-panel` vanilla-JS pattern in `base.html`, same show/hide delegated-click approach as the Player Card's `.pc-tab`, scoped per-`.card` so multiple instances wouldn't cross-toggle) using the real per-category data `build_scouting_report()` already computed — no new backend needed, every field already existed. Restyled Team Schedule as a row-list with W/L pill badges (`.schedule-row`/`.schedule-badge`) instead of a plain table. Real-data-only, as scoped: weather and week-over-week power-rank deltas stayed omitted (no weather system, no historical snapshot to diff against). Verified live in-browser (KC franchise, week 11): grid geometry checked via `getBoundingClientRect()` on every grid cell (Weekly Gameplan/Team Schedule/Scouting all at row 1, col1-stack starting row 2 alongside Box Score, Play-by-Play below spanning cols 2-3) — matches `App.tsx`'s layout exactly, not just eyeballed. Tab click-to-switch verified via DOM state (`hidden`/`active` toggling correctly). 900px breakpoint still collapses to one column. Full suite run once (pure UI/template chunk per §5.4's tiering). | `app/templates/dashboard.html`, `app/templates/base.html` (new CSS + tab JS), no `main.py` changes needed | M | **Done** |
| **M11** | ~~Roster correction~~ **DONE (2026-09-09)** | Real source: `RosterPage.tsx`/`RosterTable.tsx`/`FilterPanel.tsx`/`TopFreeAgentsBox.tsx`/`FindPlayerBox.tsx`. Real GET-param Filter panel (position groups, OVR/SPD/CTH/TCK ranges, Rookie age<=23 toggle, a real generated-query preview, Clear All/Apply) replacing the `alert()` stub. Team Quota pills are real links now (+ a real "ALL x/53" pill, 53 being the actual NFL roster limit, not fabricated per-team data) — **found and fixed a real pre-existing bug while wiring this up**: the pills were counted by each player's RAW granular `Position` enum value (Madden's QB/HB/FB/WR/TE/LT/LG/C/RG/RT/LE/RE/DT/LOLB/MLB/ROLB/CB/FS/SS/K/P scheme) against a minimums dict keyed by generic labels (QB/RB/WR/TE/C/G/T/DE/DT/LB/CB/S/K/P) — the mismatch meant 6 of 14 pills (RB/G/T/DE/LB/S) never matched anything and silently never rendered. New `_QUOTA_GROUP_FOR_POSITION` mapping fixes it; the Filter panel's position checkboxes and the Top Free Agents pager deliberately reuse the SAME 14-group mapping rather than Figma's separate 10-group filter list (OL/DL/ST) — a genuinely different granularity was going to be a second, real bug (a same-named `position` query param meaning two different label spaces), not a faithfulness nice-to-have, so this was simplified on purpose; more precise than Figma's own filter panel, not a regression. Table gained click-to-sort headers (GET-param, Stats' M3 precedent, not client JS) across all 19 real Figma columns (Player/OVR/POT/SPD/STR/AGI/TPW/TAC/CTH/TCK/AWR/STA/INJ/MOR/Pos/Age/Contract/Yrs/Depth), a sticky first column + horizontal scroll wrap, and an OVR color-tier badge (RosterTable.tsx's own thresholds). 8 real new attribute columns added: TPW/CTH/TCK/AWR are direct `Player` fields already in the model; TAC is a disclosed derived average of `throw_accuracy_short/mid/deep`; INJ is the real `durability` field read the direction Figma's column implies (`99 - durability`, per `Player`'s own module docstring formula); MOR is the real `morale` field (observed to be a constant `1` across every imported player — a real, if currently-boring, data-import characteristic, not a bug in this chunk); CTR shows the real `salary` field M8 already wired in elsewhere; DEP is a new real per-player depth-chart-slot label (`"QB1"`, `"QB2"`, ...) computed from the already-resolved depth chart order. Health/Trade columns stay omitted (disclosed, no in-season health-status or trade system exists). Whole table row now opens the Player Card (new base.html click-delegation on `tr[data-card]`), not just the name. Added 2 of Figma's 3 quick-access boxes with real data: Top Free Agents (OVR>=75, position-paged top-5) and Find Player (real league-wide name/position/team search, reusing the existing Player Card modal instead of Figma's own separate mock detail dialog); Trade Block box skipped, disclosed (no trade system, see the GM Desk audit note above). Verified live against a throwaway dev server on port 8020 (the other concurrent session's port-8010 server doesn't hot-reload Python route changes, only templates — confirmed this by getting a 500 there first): quota-pill bug fix, combined filter+sort, whole-row card-open, both quick-access boxes, and the FA view all checked via DOM assertions, not just a screenshot. Full suite: 248 passed, 1 skipped, run once (pure UI/template + one small, well-tested route-logic chunk — no game-state-affecting engine code touched). | `app/templates/roster.html`, `app/templates/base.html`, `app/main.py`'s roster route | L | **Done** |
| **M12** | ~~Playoffs correction~~ **DONE (2026-09-09)** | Real source: `FullPlayoffTree.tsx`, `ConferenceBracket.tsx`, `SuperBowlView.tsx`, `HuntCard.tsx`. Full Bracket rebuilt as a real connected 9-column tree (AFC-WC/DIV/CONF/Champ, SB, NFC-Champ/CONF/DIV/WC) with staggered column padding mirroring the source's own layout (no SVG connector lines -- a disclosed plain-CSS substitution, see `base.html`'s `.full-tree-grid` comment). Conference Bracket view gained the real 4th "Champion" column. Super Bowl tab gained compact AFC/NFC Wild Card side previews, a real Scouting Panel per SB team (reusing `build_scouting_report()` and the Dashboard's own `.dash-tab` widget from M10 -- no new UI pattern needed), and a real final-score summary linking to the full box score/play-by-play once played, instead of duplicating that page inline. MVP deliberately omitted and disclosed: no per-game MVP stat exists anywhere in this engine, and Figma's own MVP block is itself hardcoded demo data, not something a real API would return either. Hunt cards gained real `seed_if_made`/`gb` fields -- new `_conference_hunt_and_standings()` logic in `main.py`, disclosed as a stated simplifying convention (`8 + index` in the real wildcard-chain bubble order) rather than a second full 16-team absolute-rank computation. **A serious, unrelated bug surfaced and was fixed during this chunk's verification**: `season_state.reset_season()`/`simulate_current_week()` (called directly by 7 test files including `test_playoffs.py`) save to `save_service.DEFAULT_SAVE_PATH`, which had ZERO test isolation anywhere -- meaning `pytest tests/` itself, on every single run (including this session's own earlier M10/M11 "run the suite" verification steps), was silently overwriting Brian's real, in-progress franchise save. It went unnoticed because the live app server only loads the save once per process and never re-reads it -- the corruption sat invisible on disk until a fresh server process loaded it, which is exactly what exposed it here. Recovered by getting the still-running, still-correct live server to re-save its real in-memory state (a harmless no-op `POST /team-select` with the team it already had) rather than hand-editing the JSON -- verified real (KC 8-2, week 11, `playoffs: null`) before and after. Root cause fixed for good with a new `tests/conftest.py`: a session-scoped `autouse` fixture that redirects `save_service.DEFAULT_SAVE_PATH` to a throwaway file for the whole pytest run, closing this for all 7 affected files (and any future one) in one place rather than patching each test individually. Verified the fix by recording the real save file's mtime before and after a full suite run -- unchanged. Also fixed 2 stale test assertions in `test_playoffs.py` that checked for "American Conference"/"National Conference" prose headers the old (non-Figma-accurate) Full Bracket view had -- the real Figma source has no such text, so these were updated to check for the new tree's own real markers ("AFC Champ"/"NFC Champ") instead. Full suite: 248 passed, 1 skipped, confirmed via a real save-file mtime check that the isolation fix actually holds. | `app/templates/playoffs.html`, `app/templates/base.html`, `app/main.py`, `tests/test_playoffs.py`, **new** `tests/conftest.py` | L | **Done** |
| **M13** | ~~Stats correction~~ **DONE (2026-09-09)** | Real source: `docs/figma-export/src/app/lib/statHierarchy.ts` (~150 stats). Added every stat that's genuinely real and just wasn't surfaced yet: Passing gained Yards/Attempt, Sacks Taken, and Passer Rating (the real standard NFL formula, computed from already-real att/cmp/yds/td/int -- not simplified); Rushing gained Fumbles Lost; two whole new categories, **Kicking** (FG att/made/%, per-bucket makes using this engine's real `<30`/`30-39`/`40-49`/`50+` buckets from M2's `KickingLine`, XP att/made/%) and **Punting** (punts, net yards, net average -- labeled NET not gross, same disclosed distinction M2 already established, no return-game sim exists to net a gross kick against); Team stats gained a real **Special Teams** category (team FG%, team net punt average). **Found or fixed 2 real, previously-unused-but-already-real data points while building this** (same pattern as M8's salary discovery): `box_score.py`'s `PassingLine.sacks`/`RushingLine.fumbles_lost` existed per-game since M2 but were never rolled up into `season_stats.py`'s `SeasonPassingLine`/`SeasonRushingLine` season totals -- added `sacks_taken`/`fumbles_lost` fields there (backward compatible with old archived-season JSON via dataclass defaults, verified against `history_store.py`'s `SeasonPassingLine(**p)` deserialization). Kicking/Punting season aggregation added locally in `main.py`'s `_stats_page_aggregates()` rather than a new `season_stats.py` function, since nothing else (`awards.py` included) needs it yet. Added the column-chooser live search box (`StatColumnChooser.tsx`'s filter) as a small vanilla-JS `input` listener in `base.html` -- pure client-side show/hide of the already-rendered checkbox grid, no reload, consistent with the Dashboard's `.dash-tab` pattern rather than a second GET round-trip for something this cheap. Deliberately NOT added (disclosed, no fabrication): QB/RB/WR advanced stats needing depth-of-target or receiver-rank data (air yards, YAC, contested catches, yards before/after contact, broken tackles -- `scouting.py`'s own documented `PlayEvent` gap), pass-rush pressure/hurries and coverage-allowed stats (no per-play assignment data modeled), and the entire Kick/Punt Returning category (no return-game simulation exists anywhere in this engine -- confirmed via the same audit that found GM Desk/Draft/Staff have no backing systems). Coach tab stays "Coming Soon", unchanged, correctly (no Coach entity exists). **Also found and fixed the M9/M10/M11 root-cause bug's full blast radius while verifying this chunk** -- see `tests/conftest.py` and the M12 entry above for the real save-file isolation incident this surfaced; M13 was verified entirely through that same now-safe path (a manually path-isolated standalone script, mirroring what `conftest.py` does for pytest itself, so as to never risk the real save again outside pytest either). Full suite: 247 passed, 1 skipped, 1 failed (a pre-existing, unrelated, machine-speed-dependent timing flake in `test_schedule_generation_reliable_across_many_seeds` -- not caused by this chunk). | `app/engine/season_stats.py`, `app/main.py` (`PLAYER_STAT_CATEGORIES`/`TEAM_STAT_CATEGORIES`/`_stats_page_aggregates`/new `_passer_rating`), `app/templates/stats.html`, `app/templates/base.html` (search JS) | M | **Done** |
| **M14** | ~~HOF correction~~ **DONE (2026-09-09)** | Real source: `HOFPage.tsx`. Member/Inductee cards now show a real structured per-category stat table (Completions/Attempts/Yards/TDs/INTs for QB, Carries/Yards/TDs for RB, Receptions/Targets/Yards/TDs for WR/TE, Solo Tackles/TFL/Sacks/INTs/Forced Fumbles/Def TDs for DEF) instead of one flattened `stat_line` string -- confirmed via `history_store.career_stats()` that the structured `Career*Line` data already existed in full, it was just being collapsed into a single formatted string (`hall_of_fame()`'s `stat_fmt` lambdas) before ever reaching the template; new `HOFCandidate.stats` field (a `(label, value)` list) carries the un-collapsed version through instead, built by a parallel `stats_fn` lambda per position category. Also added: Figma's real column caps (3 for the Members grid, 2 for Inductees -- `.hof-grid.members`/`.hof-grid.inductees` vs. the shared uncapped `auto-fill` other grids on this page still use) and the "Strong Candidate" badge for eligible candidates at `progress_pct >= 75` (real, from data already computed). Kicking wasn't added to the per-category breakdown since M2's per-game Kicking box-score line still isn't rolled into a season/career total anywhere (the same pre-existing, disclosed gap M5's original League Record Book section already flagged) -- not a new gap this chunk introduced. Induction year deliberately NOT added, disclosed in both `HOFCandidate`'s own docstring and the template: deriving it precisely means re-running `hall_of_fame()` over every prior truncated history to find each member's first qualifying season (the same technique `_hof_new_inductee_keys()` already uses for exactly ONE season), real but non-trivial work this chunk didn't scope -- not something to fabricate a placeholder year for. Verified live via a manually-isolated standalone script (3 full simulated seasons -> 19 real inductees) that the stat table renders correct real career totals per member card, and that the real save file's mtime was unaffected throughout. Full suite: 248 passed, 1 skipped. | `app/services/history_store.py` (`HOFCandidate`/`hall_of_fame()`), `app/templates/hof.html`, `app/templates/base.html` (grid-cap + badge CSS) | S–M | **Done** |
| **M15** | ~~Depth Chart correction~~ **DONE (2026-09-09)** | Real source: `AutoFillModal.tsx` + `mockDepthChartApi.ts`'s `autoFillDepthChart()`. Added a real Auto-Fill (new `depth_chart_overrides.auto_fill()` + `POST /depth-chart/{team_abbr}/auto-fill`): a real comparator mirroring the source's exact sort shape (OVR desc, with a fatigue-aware tiebreak preferring higher STA within 2 OVR of a rival, then AWR, then STA outright, then a durability tiebreak, then last name). Only 2 of the source's 4 toggles are offered, disclosed rather than faked: **Respect Fatigue** (real, uses the real `stamina` field) and **Lock Starters** (real, keeps whoever holds each position's real starter slot(s) and only re-sorts backups) -- **Respect Injuries** (needs an in-season health-status field Player doesn't have) and **Allow Cross-Training** (needs a secondary/cross-train position concept Player doesn't have) are both omitted with an on-page disclosure, not fabricated. Player rows in each position table gained the 6 real attribute columns the audit flagged as unused (jersey number, SPD, STR, AGI, AWR, STA) alongside the existing Age/OVR/POT. KR/PR slots and in-season health badges stay correctly out of scope (no real data model for either). Verified: the real comparator against KC's actual QB roster (confirmed real OVR-descending order), the auto-fill route end-to-end (200/303 status), and the page's new columns -- all via fully-isolated standalone scripts (`save_service.DEFAULT_SAVE_PATH` + `depth_chart_overrides.DEFAULT_PATH` redirected; `depth_chart_overrides.auto_fill()`'s own writes never touch `app.core.db` at all, only its own separate `data/saves/depth_chart_overrides.json` file, so this chunk's testing was never at risk from the M14 incident described above it). Full suite: 248 passed, 1 skipped, confirmed against the freshly-restored player database. | `app/templates/depth_chart.html`, `app/main.py` (new `depth_chart_auto_fill` route), `app/services/depth_chart_overrides.py` (`auto_fill`/`_compare_for_autofill`) | M | **Done** |

---

## 3. M8 decision — resolved 2026-09-09

**This section is now historical** (kept for the record of how the decision actually played out; M8 itself is done, see §2).

This section originally framed M8 as a choice between (a) leaving the Contract tab stubbed until R2's real Contracts system builds it, or (b) a disclosed synthetic contract generator — both premised on "there's no contract data model at all — no salary, no years remaining, nothing." That premise was stale: `Player.salary`/`Player.signing_bonus` (real, non-fabricated, CSV-imported dollar figures) already existed in the data model, just unwired to any UI and — it turned out — silently broken at import time (every existing save's values were 0 due to a CSV-key bug, fixed as part of M8). Brian chose a third option once that was surfaced: show the real salary/signing bonus data, disclose that contract length/years-remaining/cap space still aren't modeled. See §2's M8 row for what was actually built.

**Lesson for future sessions:** before presenting a roadmap "decision needed" framing as fact, verify the underlying premise against the actual current code/data (a quick grep on the model + a check of whether the CSV/import path actually populates it) rather than trusting an earlier session's writeup — roadmap prose can go stale exactly like this.

---

## 4. R-series (Post-MVP) chunks

Bigger, more architecturally significant. Each major chunk is broken into sub-chunks (a/b/c/d) sized for individual fresh-session work — don't try a whole R-chunk in a single sitting.

| # | Chunk | Scope | Size | Model | Sub-chunks |
|---|---|---|---|---|---|
| **R1** | Injury system | Real in-season injury events tied to `durability`. **Directly synergizes with item 37's rotation.py** — a player going down should increase the backup's `reliability_factor`/share for the rest of that game, which the rotation math already supports. Do this AFTER M1-M6, before R2/R4/R5 (it'll change how those get built). | L | **Opus** for design (touches rotation.py, box scores, progression, roster pages all at once), Sonnet for implementation | R1a (design), R1b (core sim), R1c (rotation), R1d (integration) |
| **R2** | Return-game simulation | Punt/kickoff return yardage. Unlocks a real (not scoped-narrow) Defensive TD, real special-teams tackle credit (the biggest reason DEF tackles are still ~2-3x real — see `test_stat_realism.py`'s disclosed gap), and completes M2's punt stats. | L | Opus for design, Sonnet for implementation | R2a (design), R2b (core), R2c (stats) |
| **R3** | Coaching Staff | New `Coach` entity, hiring/firing, real Staff page. Unlocks Coach of the Year (currently impossible — no Coach entity exists). | L | Opus for design, Sonnet for implementation | R3a (design), R3b (entity), R3c (UI) |
| **R4** | Contracts / Free Agency / Trades | Genuinely the biggest single chunk in R2. **Split into three sessions, in order**: R4a Contracts (salary cap, negotiation — also closes M8 for real), R4b Free Agency, R4c Trades. Unlocks real GM Desk content. | XL (3 sessions) | Opus for each sub-chunk's design, Sonnet for implementation | R4a, R4b, R4c (separate chunks) |
| **R5** | Draft | Draft classes, draft-day logic, real Draft page. Depends on R4a (rookie contracts) existing first. | L | Opus for design, Sonnet for implementation | R5a (design), R5b (core), R5c (UI) |
| **R6** | Full penalty catalog | Expand from 7 to the GDD's full dozen-plus types. Mechanical extension of the existing, well-documented pattern in `drive_sim.py`'s Penalty System section. | S–M | Sonnet | Can bundle with R7 |
| **R7** | Weather | Weather modifiers (§6.11). | S | Sonnet | Can bundle with R6 |
| **R8** | Awards page (dynamic, weekly MVP/OPOY/DPOY/ROY/COY) | Your idea from earlier this session — overlaps with the existing HOF and Stats Awards Race, needs a real scoping conversation before any code (not a "just build it" chunk). Do this scoping as the FIRST five minutes of whatever session picks it up, not blind. | M | Sonnet, after scoping | Scoping-only |
| **R9** | Weekly Headlines (GDD §12) | Real spec: `docs/GDD_v3.2.md` §12 (added 2026-09-09; §12.3's scoring/selection algorithm is still a placeholder there -- get the rest of that section from Brian before starting). GDD labels this "(MVP)", but flagging a real scoping question for whoever picks it up: this is the **first feature in the whole project that calls an external API** (Claude, for headline generation) -- every other system here is fully deterministic and offline (GDD §1.3's own Determinism & Seeding Policy, "given the same LEAGUE_SEED... results must be perfectly reproducible"). An LLM-generated headline is neither seeded nor reproducible by nature, so this needs an explicit decision on how it coexists with that policy (e.g.: headlines are cosmetic/display-only and never feed back into sim state, cached in the save file post-generation so a reload doesn't regenerate/change them, network failure falls back to a plain templated headline instead of blocking the dashboard) before real design work starts. Also needs: the event-detection pass itself (Tier 1/2/3 scoring against real box-score/standings data, all of which already exists and is real -- this part is a normal engine module, not novel), and a plan for API-key config/secrets (this project has none today). | L | **Opus for design** (novel architecture decision + a determinism-policy exception, same bar as R1-R5's design phase) | R9a (event-detection + tiering, no API), R9b (Claude integration + caching + offline fallback), R9c (Dashboard UI) |

**Recommended R-series order:** R1a→R1b→R1c→R1d (injury system, builds foundation) → R2a→R2b→R2c (return game, unlocks DEF-tackle fix) → R3a→R3b→R3c (coaching) → R4a (contracts, blocks R5) → R4b (free agency) → R4c (trades) → R5a→R5b→R5c (draft, depends on R4a) → R6+R7 (bundle small chunks) → R8 (after scoping) → R9 (after the determinism-policy question is settled).

---

## 5. How to actually run this efficiently (the token-saving playbook)

This session (the one that produced this file) is enormous — every remaining turn in it re-sends its entire history. That's the single biggest thing to avoid going forward.

1. **One chunk = one fresh Claude Code chat.** Don't continue this session for the next chunk. A new chat starts with an empty context window; it only grows with what that chunk actually needs.

2. **Open each new session with a tight, direct prompt** — not "explore the codebase and figure out what to do." Use this template:

   > Read `HANDOFF.md` and `ROADMAP.md`. Implement chunk **[M1/M2/M3/...]** as scoped in ROADMAP.md §2 (or §4). Relevant files: [copy the "Key files" column]. Follow the existing patterns in those files — don't redesign anything not in scope. Run the test suite before committing.

   Naming the exact chunk ID and files means the fresh session doesn't spend tokens re-discovering what this file already tells it.

3. **Match the model to the chunk** (see the tables above):
   - **Sonnet 5** (this session's model) — the right default for most implementation work. Good capability-to-cost balance.
   - **Haiku 4.5** — for mechanical, low-ambiguity chunks where the hard thinking is already done (M3, M4: a real Figma source file just needs translating into the existing template pattern). Meaningfully cheaper; don't pay Sonnet/Opus prices for a translation job.
   - **Opus** — reserve for chunks with real design risk: a novel mechanic touching multiple systems at once (M1, R1, R2, R3, R4, R5's design phase), or untangling a gnarly bug (like this session's stat-realism investigation, which took real iterative reasoning). Costs more per token, but a chunk like that done wrong in Sonnet often costs MORE overall once you count the rework.

4. **Tier your test-verification effort by risk, don't run the full twice-suite for everything:**
   - Engine/stats-affecting chunks (M1, M2, R1-R5): keep the full "run the suite twice" discipline — these can silently corrupt stats the way the box_score.py rushing bug did this session.
   - Pure UI/template chunks (M3, M4, M5, M6, R8): one test run + one live-browser check via the preview tools is enough. Don't burn tokens re-running an 8-minute full suite twice for a template change that can't touch game state.

5. **Bundle small chunks into an already-warm session.** Once a session has HANDOFF.md/ROADMAP.md loaded, the dev server running, and context established, a second small chunk in the same session is cheaper than a second fresh session's startup cost. M5+M6 and R6+R7 are natural pairs.

6. **Don't re-run the stat-realism scratch-audit scripts from this session.** `tests/test_stat_realism.py` is now a permanent, checked-in regression gate — trust it. Only reach for a fresh one-off audit script if you're investigating a genuinely NEW reported anomaly, the way this session's work started.

7. **Point new sessions at real source, don't make them re-derive it.** The Figma export is already extracted to `docs/figma-export/` in the repo — a fresh session should read the specific `.tsx` file for its page, not re-request the zip or guess at layout from screenshots.

8. **Keep chunks scoped exactly as written here — resist scope creep mid-session.** The reason M1 says "no open-field return simulation" is specifically to stop it from silently ballooning into R2's territory. If a session doing M1 starts wanting to build return yardage too, that's the moment to stop and open a fresh R2 session instead, not push through in the same one.

9. **Use `/loop` or extended autonomous work only for chunks that are already well-scoped** (i.e., anything in the tables above). Open-ended "keep improving things" autonomous sessions are the most expensive mode there is, because most of the token spend goes to self-directed exploration rather than building.
